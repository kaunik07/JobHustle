'use client';

// This component is no longer used and is replaced by dedicated pages for editing LaTeX resumes.
// It is kept to avoid potential build errors if it were imported elsewhere unexpectedly, but it does nothing.
export function LatexResumeEditorDialog() {
  return null;
}
